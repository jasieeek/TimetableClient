import {WeekDay} from '@angular/common';

export class Lesson {
  id: string;
  name: string;
  teacherName: string;
  classroomName: string;
  className: string;
  dayOfWeek: string;
  sequence: number;
}
